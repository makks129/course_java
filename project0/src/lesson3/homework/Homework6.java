package lesson3.homework;

/**
 * Shifting elements in an array.
 *
 * - Create an array
 * - Fill it with integers
 * - Iterate through the array and shift elements to the left (swap an element with an element on the left of it)
 * - Iterate through the array and shift elements to the right (swap an element with an element on the right of it)
 * - After both operations you should have the same array as before the operations
 */
public class Homework6 {

    public static void main(String[] args) {



    }

}
